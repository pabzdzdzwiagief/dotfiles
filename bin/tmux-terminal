#!/usr/bin/env sh

SESSION=urxvt
exec urxvtc -title urxvt -e sh -c "tmux has-session -t$SESSION && \
                                   exec tmux attach-session -t$SESSION || \
                                   exec tmux new-session -s$SESSION"
